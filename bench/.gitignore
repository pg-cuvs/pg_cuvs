__pycache__/
*.pyc
data/
# Generated run output: ignored by default so a stray run never lands in a commit.
# Curated artifacts are added deliberately with `git add -f` and listed in
# results/README.md (the provenance ledger). Docs in results/ are tracked normally.
results/*.csv
results/*.out
results/*.log
